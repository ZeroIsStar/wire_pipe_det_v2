[YOLO检测成功] -> [转换到map坐标] -> [与缓存比较，距离<0.3m则更新/刷新，否则新增]
                    |
                    v
[定时器触发(每100ms)] 
                    |
                    v
[遍历缓存] -> 计算到路径距离 -> (距离>1.0m && 超过5秒未看见) -> [删除该记忆]
                    |
                    v
[再次遍历剩余缓存] -> 是否存在距离<=1.0m的? 
                    |
                    |-- Yes --> [触发警报，记录当前时间]
                    |            |
                    |            v
                    |       [状态维持2秒]，发布 true
                    |
                    |-- No  --> [检查距离上次触发是否>2秒] -> 是则发布 false
